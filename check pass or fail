#!/bin/bash

# Threshold for passing grade
pass_threshold=60

# Prompt the user to enter the grade for the subject
echo "Enter the grade for the subject:"
read grade

# Check if the grade is greater than or equal to the passing threshold
if [ "$grade" -ge "$pass_threshold" ]; then
    echo "Pass"
else
    echo "Fail"
fi
